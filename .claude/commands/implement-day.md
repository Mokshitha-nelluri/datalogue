Implement Day $ARGUMENTS from the build plan in @querymind-project-brief.md.

Before any code:
1. Re-read the Day $ARGUMENTS section of the brief
2. List every file you will create or modify
3. Write all types and interfaces first
4. Write tests first — TDD
5. Implement until tests pass
6. Run pnpm typecheck — fix all errors before continuing
7. Suggest the git commit message

If anything in the brief is ambiguous, ask before implementing.